@import '../../../common/style/mixin.styl'

.dialog
  position fixed
  display flex
  flex-direction column
  z-index 100
  background $chat-bgcolor
  top 0
  bottom 0
  left 0
  right 0
  .top-bar-wrapper
    flex 0 0 auto
  .dialog-box-wrapper
    flex 1
    overflow-y hidden
  .input-bar
    flex 0 0 auto